from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='An attempt to find the best airbnb listing under price constraints.',
    author='kailu3',
    license='MIT',
)
